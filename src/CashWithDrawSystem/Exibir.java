package CashWithDrawSystem;

public class Exibir {
    public static void showAmountNote(int amountBank, int note ){
        if(amountBank != 0){
            System.out.println("Seu valor guarda: " + amountBank + " notas de " + note);
        }
    }
}
